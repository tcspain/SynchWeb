/*
 * View an XPDF stage 
 */

define([
        "marionette",
        "modules/shipment/views/containerplate",
        ], function(
        Marionette,
        GenericPlateView
        ) {
	return GenericPlateView;
});